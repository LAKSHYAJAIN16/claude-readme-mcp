# docs

Screenshots and any longer-form documentation for this project live here, separate from the top-level README so that stays short.

- `screenshot.png` (or similar) — where the `readme-writer` skill saves a captured screenshot when `requireScreenshots` is on, per `skills/readme-writer/SKILL.md`.
